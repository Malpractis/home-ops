---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/volsync.backube/replicationdestination_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: "${APP}-${PVC_NAME}-dest"
  labels:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent
spec:
  trigger:
    manual: restore-once
  restic:
    repository: "${APP}-${PVC_NAME}-volsync-b2-secret"
    copyMethod: "${VOLSYNC_COPYMETHOD:=Snapshot}"
    # Capacity
    capacity: "${VOLSYNC_CAPACITY:=10Gi}"
    cacheCapacity: "${VOLSYNC_CACHE_CAPACITY:=10Gi}"
    # Storage Classes
    storageClassName: "${VOLSYNC_STORAGECLASS:=longhorn-snapshot-sc}"
    volumeSnapshotClassName: "${VOLSYNC_SNAPSHOTCLASS:=longhorn-snapshot-vsc}"
    cacheStorageClassName: "${VOLSYNC_CACHE_STORAGECLASS:=longhorn-cache-sc}"
    # Access Modes
    accessModes: ["${VOLSYNC_ACCESSMODES:=ReadWriteOnce}"]
    cacheAccessModes: ["${VOLSYNC_CACHE_ACCESSMODES:=ReadWriteOnce}"]
    moverSecurityContext:
      runAsUser: ${VOLSYNC_PUID:=568}
      runAsGroup: ${VOLSYNC_PGID:=568}
      fsGroup: ${VOLSYNC_FS_PGID:=568}
    enableFileDeletion: true
    # True for Rook-Ceph, false for Longhorn
    cleanupCachePVC: false
    cleanupTempPVC: false
