---
# yaml-language-server: $schema=https://version-2026-5.goauthentik.io/blueprints/v1/schema.json
# All native-OIDC (OAuth2) applications. Proxy/forward-auth apps live in
# 16-apps-proxy-media.yaml / 17-apps-proxy-admin.yaml; the embedded outpost is 18.
# NOTE: this is a single shared failure domain — a bad entry (e.g. a !Find that can't
# resolve, or a missing !Env secret) can block this whole blueprint from applying.
version: 1
metadata:
  name: Materia - Apps - OIDC
entries:
  # ════════════════════════════════════════════════════════════════════════════
  # Infra
  # ════════════════════════════════════════════════════════════════════════════

  # ── Grafana ───────────────────────────────────────────────────────────────────
  - id: grafana_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: grafana
    attrs:
      client_id: !Env GRAFANA_CLIENT_ID
      client_secret: !Env GRAFANA_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://grafana.materia.wtf/login/generic_oauth
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "groups"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: grafana_application
    model: authentik_core.application
    identifiers:
      slug: grafana
    attrs:
      name: Grafana
      provider: !KeyOf grafana_provider
      group: infra
      meta_launch_url: https://grafana.materia.wtf
      meta_description: "Metrics & dashboards"
      meta_icon: application-icons/svg/grafana.svg
      open_in_new_tab: true

  - id: grafana_admins_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf grafana_application
      group: !Find [authentik_core.group, [name, "infra"]]
    attrs:
      order: 0

  # ── TrueNAS ───────────────────────────────────────────────────────────────────
  - id: truenas_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: truenas
    attrs:
      client_id: !Env TRUENAS_CLIENT_ID
      client_secret: !Env TRUENAS_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://nas.materia.wtf/api/v2.0/auth/oidc_callback
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: truenas_application
    model: authentik_core.application
    identifiers:
      slug: truenas
    attrs:
      name: TrueNAS
      provider: !KeyOf truenas_provider
      group: infra
      meta_launch_url: https://nas.materia.wtf
      meta_description: "NAS & storage"
      meta_icon: application-icons/svg/truenas-scale.svg
      open_in_new_tab: true

  - id: truenas_admins_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf truenas_application
      group: !Find [authentik_core.group, [name, "infra"]]
    attrs:
      order: 0

  # ════════════════════════════════════════════════════════════════════════════
  # Media
  # ════════════════════════════════════════════════════════════════════════════

  # ── Seerr ─────────────────────────────────────────────────────────────────────
  - id: seerr_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: seerr
    attrs:
      client_id: !Env SEERR_CLIENT_ID
      client_secret: !Env SEERR_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://requests.materia.wtf/auth/oidc-callback
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: seerr_application
    model: authentik_core.application
    identifiers:
      slug: seerr
    attrs:
      name: Seerr
      provider: !KeyOf seerr_provider
      group: members
      meta_launch_url: https://requests.materia.wtf
      meta_description: "Media requests"
      meta_icon: application-icons/png/seerr.png
      open_in_new_tab: true

  - id: seerr_members_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf seerr_application
      group: !Find [authentik_core.group, [name, "members"]]
    attrs:
      order: 0

  - id: seerr_media_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf seerr_application
      group: !Find [authentik_core.group, [name, "media"]]
    attrs:
      order: 10

  # ── qui (autobrr suite) ─────────────────────────────────────────────────────────
  - id: qui_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: qui
    attrs:
      client_id: !Env QUI_CLIENT_ID
      client_secret: !Env QUI_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://qui.materia.wtf/api/auth/oidc/callback
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "groups"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: qui_application
    model: authentik_core.application
    identifiers:
      slug: qui
    attrs:
      name: qui
      provider: !KeyOf qui_provider
      group: media
      meta_launch_url: https://qui.materia.wtf
      meta_description: "Torrent management"
      meta_icon: application-icons/svg/qbittorrent.svg
      open_in_new_tab: true

  - id: qui_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf qui_application
      group: !Find [authentik_core.group, [name, "media"]]
    attrs:
      order: 0

  # ── autobrr ─────────────────────────────────────────────────────────────────────
  # NOTE: the old autobrr forward-auth proxy provider + application were deleted
  # manually in the Authentik UI during migration. The OIDC application reuses slug
  # "autobrr".
  - id: autobrr_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: autobrr
    attrs:
      client_id: !Env AUTOBRR_CLIENT_ID
      client_secret: !Env AUTOBRR_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://autobrr.materia.wtf/api/auth/oidc/callback
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: autobrr_application
    model: authentik_core.application
    identifiers:
      slug: autobrr
    attrs:
      name: autobrr
      provider: !KeyOf autobrr_provider
      group: media
      meta_launch_url: https://autobrr.materia.wtf
      meta_description: Torrent automation
      meta_icon: application-icons/svg/autobrr.svg
      open_in_new_tab: true

  - id: autobrr_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf autobrr_application
      group: !Find [authentik_core.group, [name, "media"]]
    attrs:
      order: 0

  # ── Calibre-Web-Automated ─────────────────────────────────────────────────────
  # NOTE: replaced the old calibre forward-auth proxy provider. Reuses slug "calibre";
  # on reconcile Authentik repoints the existing app at this OAuth2 provider. Delete the
  # orphaned proxy provider (named "calibre") manually in the UI (System > Providers).
  # CWA's redirect path for a generic OIDC provider is /login/generic/authorized
  # (cps/oauth_bb.py).
  - id: calibre_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: calibre
    attrs:
      client_id: !Env CALIBRE_CLIENT_ID
      client_secret: !Env CALIBRE_CLIENT_SECRET
      client_type: confidential
      redirect_uris:
        - url: https://calibre.materia.wtf/login/generic/authorized
          matching_mode: strict
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      authorization_flow: !Find [authentik_flows.flow, [slug, "default-provider-authorization-implicit-consent"]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, "materia-invalidation"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "groups"]]
      grant_types:
        - authorization_code
        - refresh_token

  - id: calibre_application
    model: authentik_core.application
    identifiers:
      slug: calibre
    attrs:
      name: Calibre
      provider: !KeyOf calibre_provider
      group: members
      meta_launch_url: https://calibre.materia.wtf
      meta_description: eBook library
      meta_icon: application-icons/svg/calibre-web.svg
      open_in_new_tab: true

  - id: calibre_members_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf calibre_application
      group: !Find [authentik_core.group, [name, "members"]]
    attrs:
      order: 0

  - id: calibre_media_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf calibre_application
      group: !Find [authentik_core.group, [name, "media"]]
    attrs:
      order: 10
