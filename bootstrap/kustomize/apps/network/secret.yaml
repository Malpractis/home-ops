---
# bootstrap seed — ExternalSecret (certificates/import) takes over once ESO is running
apiVersion: v1
kind: Secret
metadata:
  name: materia-wtf-tls
type: kubernetes.io/tls
stringData:
  tls.crt: "{{ ENV['lets_encrypt_tls_crt'] }}"
  tls.key: "{{ ENV['lets_encrypt_tls_key'] }}"
