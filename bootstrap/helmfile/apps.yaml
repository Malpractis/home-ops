---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile
bases:
  - defaults.yaml
---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile
helmDefaults:
  cleanupOnFail: true
  forceConflicts: true
  wait: true
  waitForJobs: true

releases:
  - name: cilium
    namespace: kube-system
    inherit:
      - template: default
    hooks:
      - # Wait for CRDs to be available
        command: kubectl
        args:
          - wait
          - --for=create
          - crd/ciliumbgpadvertisements.cilium.io
          - crd/ciliumbgpclusterconfigs.cilium.io
          - crd/ciliumbgppeerconfigs.cilium.io
          - crd/ciliumloadbalancerippools.cilium.io
          - --timeout=2m
        events:
          - postsync
        showlogs: true
      - # Apply Resources
        command: kubectl
        args:
          - apply
          - --namespace={{ .Release.Namespace }}
          - --server-side
          - --force-conflicts
          - --filename=../../kubernetes/apps/{{ .Release.Namespace }}/{{ .Release.Name }}/app/networks.yaml
        events:
          - postsync
        showlogs: true

  - name: coredns
    namespace: kube-system
    inherit:
      - template: default
    needs:
      - kube-system/cilium

  - name: spegel
    namespace: system
    inherit:
      - template: default
    needs:
      - kube-system/coredns

  - name: cert-manager
    namespace: security
    inherit:
      - template: default
    hooks:
      - events: [postsync]
        command: kubectl
        args:
          - apply
          - --namespace=security
          - --server-side
          - --force-conflicts
          - --filename=../../kubernetes/apps/security/cert-manager/app/clusterissuers.yaml
          - --wait=true
        showlogs: true
    needs:
      - system/spegel

  - name: external-secrets
    namespace: security
    inherit:
      - template: default
    needs:
      - security/cert-manager

  - name: onepassword-connect
    namespace: security
    inherit:
      - template: default
    needs:
      - security/external-secrets

  - name: flux-operator
    namespace: flux-system
    inherit:
      - template: default
    needs:
      - security/onepassword-connect

  - name: flux-instance
    namespace: flux-system
    inherit:
      - template: default
    needs:
      - flux-system/flux-operator
