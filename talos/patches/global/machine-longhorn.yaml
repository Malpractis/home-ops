machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/longhorn-data
        type: bind
        source: /var/mnt/longhorn-data
        options:
          - bind
          - rshared
          - rw
