# Secrets
*.pub
*.key
*.decrypted~*.yaml
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.yaml
/nodes.yaml
# Kubernetes
kubeconfig
talosconfig
# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform/**/.env
# Local-only docs (personal notes — keep out of the public repo)
/docs/
# Misc.
.claude/
.private/
.task/
.venv/
.DS_Store
Thumbs.db
