---
- name: Restart dnscrypt-proxy
  ansible.builtin.systemd:
    name: dnscrypt-proxy
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode   # unit may not exist yet on a fresh host during --check
